# Copy to your shell environment (do not commit real keys). All optional.
# Set ONE provider key; the agent falls back to offline parsing + local OCR when none is set.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
# LLM_PROVIDER=openai        # openai | anthropic | gemini
# LLM_MODEL=gpt-4o-mini
# LLM_DISABLE=1
